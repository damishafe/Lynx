# dependencies
node_modules/

# env / secrets
.env*
!.env.example

# OS / editor
.DS_Store

# ProofLoop runtime artifacts (run reports, copied evidence)
.proofloop/

# Kane CLI local scratch
.testmuai/tests/

# git worktrees / superpowers scratch
.worktrees/
.superpowers/

# Kane evidence packs (large; served locally via `kane-cli evidence serve`)
.testmuai/evidence/

# demo film workspace (Remotion, raw captures, renders)
video/
